apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: workshop-postgres
spec:
  instances: 3
  storage:
    size: 20Gi
  resources:
    requests:
      memory: "2048Mi"
      cpu: "2"
    limits:
      memory: "8192Mi"
      cpu: "6"

