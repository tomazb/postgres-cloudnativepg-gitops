apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: workshop-postgres
spec:
  instances: 2
  resources:
    requests:
      memory: "2048Mi"
      cpu: "2"
    limits:
      memory: "8192Mi"
      cpu: "8"

