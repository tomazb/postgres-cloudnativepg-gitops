apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: workshop-postgres
spec:
  instances: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "0.1"
    limits:
      memory: "512Mi"
      cpu: "0.5"

